# Create a tuple with 4 colors
colors = ("red", "green", "blue", "yellow")

# Print the second color
print(colors[1])
# Print the last color using negative indexing
print(colors[-1])

# Get number of elements
no_of_items = len(colors)
print(no_of_items)

data = (4, 7, 4, 9, 4, 2)

# Print how many times 4 appears
print(data.count(4))
# Print the index of the first 9
print(data.index(9))
# Print the total number of elements
print(len(data))